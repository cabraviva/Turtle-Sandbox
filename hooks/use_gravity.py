from objects import Object

def use_gravity(strength: float):
    def hook_fn(obj: Object):
        # print("DEBUG: Applying gravity to:")
        # print(obj)
        while not_touching_ground:
            fall_down(strength cm)
    return hook_fn